// import { ROUTING } from '../types/routing';
// import { browserHistory } from 'react-router';

// export const redirect = (store) => {
//     (next) => {
//         return (action) => {
//             if (action.type === ROUTING) {
//                 createh[action.payload.method](action.payload.nextUrl)
//             }
//         }
//     }

//     return next(action);
// }