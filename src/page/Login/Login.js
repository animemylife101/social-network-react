import React from 'react';
import LoginForm from '../../page/Login/LoginForm/LoginForm';

const Login = (props) => {
    return <div>
        <LoginForm {...props} />
    </div>
}

export default Login;